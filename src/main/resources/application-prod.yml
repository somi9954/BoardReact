server:
  port: 3000

spring:
  servlet:
    multipart: # 파일 업로드 설정
      maxFileSize: 5MB
      maxRequestSize: 10MB

  datasource: # 데이터 베이스 연결 설정
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://somi-mysql.cb4mkq0amnvs.ap-northeast-2.rds.amazonaws.com:3306/boardproject
    username: admin
    password: Ranhee9954!

  jpa: # JPA 설정
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        use_sql_comments: true
        dialect: org.hibernate.dialect.MySQLDialect
    hibernate:
      ddlAuto: create

  logging:
    level:
      org:
        hibernate:
          type:
            descriptor:
              sql: trace

# 파일 업로드 경로 설정
file:
  upload:
    path: C:/uploads/
    url: /uploads/